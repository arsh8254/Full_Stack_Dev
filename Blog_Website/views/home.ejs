<!-- contains the full path of header file -->
<%- include("partials/header"); -%>
    <h1>Home</h1>
    <p> <%= startingContent %> </p>

  <%  posts.forEach(function(post){ %>
    <h1><%=post.title%></h1>
    <!-- to truncate the post strings and add link to post page -->
    <p>
    <%=post.content.substring(0, 100) + " ..."%>
    <a href="/posts/<%=post._id%>">Read More</a>
    </p>
    <% }) %>

<!-- contains the full path of footer file -->
<%- include("partials/footer"); -%>
